# Core dependencies (required)
flask==3.1.0
flask-cors==5.0.1
python-dotenv==1.0.0
gunicorn==21.2.0
requests==2.31.0
pandas==2.2.0
numpy==2.2.0
pytz==2024.1
python-dateutil==2.8.2
werkzeug==3.1.0
alpaca-trade-api==3.2.0

# Whitenoise for serving static files in production
whitenoise==6.5.0

# Optional dependencies (will be mocked if not available)
# Comment out or remove dependencies that cause issues in your environment
# Data sources
alpaca-py==0.38.0
polygon-api-client>=1.14.2 ; platform_system != "Render"  # For market data

# AI components
openai==1.5.0

# UI and notifications
pygame>=2.5.0 ; platform_system != "Render"
pyttsx3>=2.90 ; platform_system != "Render" 

# Data analysis and visualization
matplotlib==3.9.0
plotly>=5.15.0 ; platform_system != "Render"
scikit-learn==1.4.0
seaborn==0.13.0
scipy==1.12.0
beautifulsoup4==4.12.2
yfinance==0.2.35

# Utilities
tqdm>=4.65.0
pydantic==2.5.3
peewee==3.17.0
PyYAML>=6.0

# Testing
pytest==7.3.1 ; platform_system != "Render"
pytest-cov==4.1.0 ; platform_system != "Render"

# Windows-specific (conditional)
pywin32>=303; platform_system=="Windows"

# Add basic web functionality
blinker>=1.4.0
markupsafe>=2.0.0

# Add database functionality
# sqlalchemy>=1.4.0
# peewee>=3.14.0

# Note: The platform_system != "Render" condition will skip installation on Render
# The mock_modules system will create mock implementations for these packages

# New dependencies from the code block
newsapi-python==0.2.7
aiohttp==3.9.0
websockets==12.0
discord-webhook==1.3.0
