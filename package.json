{
  "name": "ai-trading-bot",
  "version": "1.0.0",
  "description": "AI Trading Bot with React frontend and Python backend",
  "scripts": {
    "dev": "echo 'Use Python virtual environment for development'",
    "start-frontend": "cd frontend && npm start",
    "start-api": "python start-api.py",
    "install-api-deps": "pip install flask flask-cors pandas",
    "start": "node server.js",
    "build": "python -m venv .venv && .venv/bin/pip install -r requirements.txt && python copy_config_files.py",
    "postinstall": "npm install express || echo 'Express installation failed'"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "recharts": "^2.15.2"
  },
  "devDependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/icons-material": "^7.0.2",
    "@mui/material": "^7.0.2"
  }
}
